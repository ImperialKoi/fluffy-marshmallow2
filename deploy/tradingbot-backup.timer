# OPTIONAL: run the S3 state backup once a day (and shortly after boot if missed).
# Enable: sudo systemctl enable --now tradingbot-backup.timer
[Unit]
Description=Daily S3 backup of trading-bot state

[Timer]
OnCalendar=*-*-* 21:30:00
Persistent=true

[Install]
WantedBy=timers.target
